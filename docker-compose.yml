services:
  ffmpeg-worker:
    build: .
    ports:
      - "${HOST_PORT:-8000}:8000"
    volumes:
      - ${DATA_VOLUME_PATH:-./data}:/data
    environment:
      - DATA_PATH=/data
      - FFMPEG_TIMEOUT=${FFMPEG_TIMEOUT:-3600}
